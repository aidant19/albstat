package albstat;

import static org.junit.Assert.assertTrue;

import org.junit.Test;

/**
 * idk if i need this or not but i just kept it in so i don't have to think about it
 */
public class AppTest 
{
    /**
     * Rigorous Test :-)
     */
    @Test
    public void shouldAnswerWithTrue()
    {
        assertTrue( true );
    }
}
